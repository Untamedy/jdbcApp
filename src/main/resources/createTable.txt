CREATE TABLE IF NOT EXISTS mydb.address(id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,street varchar(30) NOT NULL,buildNum int NOT NULL)
CREATE TABLE IF NOT EXISTS mydb.flats (id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,region varchar(50) NOT NULL,addressId int NOT NULL,room int NOT NULL,square int NOT NULL, price int NOT NULL)
